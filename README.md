How to run this lab

1. Open terminal in the project root
2. Move into the Django project folder:
   cd mysite
3. Install dependencies:
   pip install -r requirements.txt
4. Run tests:
   python manage.py test